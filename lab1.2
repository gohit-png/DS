
#include <stdio.h>
#define  size 100
struct student
{
    int id;
    int age;
    int marks;
};
struct student s1[size];
void accept_input(struct student *s1)
{
   printf("enter the id\n");
   scanf("%d",&s1->id);
       printf("enter the age\n");
   scanf("%d",&s1->age);
      printf("enter the marks\n");
   scanf("%d",&s1->marks);
}
void print_input(struct student *s1)
{
printf("student details are \n;");
printf(" id %d\n",s1->id);
 printf(" the age %d\n",s1->age);
 printf(" the marks %d\n",s1->marks);
}
