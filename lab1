#include <stdio.h>

void swap(int *a,int *b)
    {
        int temp;
    temp=*a;
    *a=*b;
   *b=temp;
    }
    
int main() 
{
    int x,y;
    printf("enter val of x and y \n");
    scanf("%d %d",&x,&y);
    swap(&x,&y);
    printf("after swapping numbers are %d,%d",x,y);
    return 0;
}  


